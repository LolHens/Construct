primitive_types: Int, Bool, Float
Option (nullable)
return
if
break
continue
throw

priority:
if
primitive types


{
val | expr
}

val = "val" text "=" expr

expr = blockWith | blockWithEval

val = expr: UNIT????


Typesystem special cases:
Nothing,
Unit,
Void,
And?,
Or?